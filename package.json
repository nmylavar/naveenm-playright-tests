{
  "name": "chevy-playwright-tests",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:debug": "PWDEBUG=1 playwright test",
    "setup-auth": "npx tsx scripts/setup-auth.ts",
    "setup-auth:manual": "npx tsx scripts/setup-auth-manual.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "typescript": "^5.6.0",
    "tsx": "^4.7.0"
  }
}
