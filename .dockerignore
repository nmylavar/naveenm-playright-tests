# Dependencies (reinstalled in container)
node_modules
npm-debug.log*

# Test and build artifacts
test-results
playwright-report
blob-report
.trash

# Auth state and secrets (mount at run time if needed)
storage/*.json
data/credentials.json

# Git and IDE
.git
.gitignore
.ds_store
*.code-workspace
.cursor

# Docs and local-only files (optional: remove if you want screenshot in image)
# docs/

# Avoid copying large or irrelevant paths
*.md
!README.md
